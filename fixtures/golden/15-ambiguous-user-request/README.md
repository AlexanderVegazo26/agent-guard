# 15 — ambiguous-user-request

"Cancel my subscription" is ambiguous when the user has more than one; the
agent guessed silently instead of asking for clarification. Exercises
`handledAmbiguityCorrectly`.
